#include <stdio.h>

int main()
{
    int a1, a2, r1;
    
    a1 = 3;
    a2 = 4;
    
    pt= a + b sqrt( pow(b,2) + pow(b,2) + pow(a,2) );
    at= b*a/2;
    
    printf("El area total es %f\n", at);
    printf("El perímetro total es %f\n");
    
    return 0;
}